# Grão

**Leia `CONTEXTO.md` antes de alterar qualquer coisa em `src/`.**

Ele descreve os dois produtos (devocional gratuito e semente do assinante), o
contrato da API, o fluxo do onboarding e as regras que não podem ser
quebradas: o paywall é do servidor, o app nunca escreve versículo bíblico, a
resposta de segurança emocional não se altera, e `server/` não se mexe.

# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v55.0.0/ before writing any code.
