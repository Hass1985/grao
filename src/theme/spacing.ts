export const space = {
  xs: 8,
  sm: 12,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
  /** Margem horizontal padrão das telas (header, logo, progresso). */
  gutter: 22,
};

/** Slot do logo Grão — mesma geometria em BackButton e AppHeader. */
export const logoSlot = 40;
export const logoSize = 22;
